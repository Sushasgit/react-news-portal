import React, { Fragment } from 'react';
import ArticlesList from '../../components/ArticlesList';

const Home = () => (
    <Fragment>
        <ArticlesList view="full" />
    </Fragment>
);

export default Home;
