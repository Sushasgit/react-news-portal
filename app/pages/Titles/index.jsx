import React, { Fragment } from 'react';
import ArticlesList from '../../components/ArticlesList';

const Titles = () => (
    <Fragment>
        <ArticlesList view="short" />
    </Fragment>
);

export default Titles;
